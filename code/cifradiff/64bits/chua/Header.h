#include <math.h>

//typedef double data_t;
//void RK4_LBE();
const double h = (1.0) * pow(10.0, -6.0);
const double C1 = 10.0 * pow(10.0, -9.0);
const double C2 = 100.0 * pow(10.0, -9.0);
const double L = 19.0 * pow(10.0, -3.0);
const double R = 1800.00;
const double m0 = -0.37 * pow(10.0, -3.0);
const double m1 = -0.68 * pow(10.0, -3.0);
const double Bp = 1.1;
const double eq1 = (1.0 / (10.0 * pow(10.0, -9.0)));
const double eq2 = (1.0 / (100.0 * pow(10.0, -9.0)));
const double eq3 = -(1.0 / (19.0 * pow(10.0, -3.0)));
const double yo = -0.2;
const double zo = 0.0;
